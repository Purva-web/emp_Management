PROCEDURE "EMP_DATA" (
	-- IN Parameters
	IN ST_EMP_DATA "ST_EMP_DATA",
	OUT OUT_SUCCESS VARCHAR(100)
)
	LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER 
   --DEFAULT SCHEMA <default_schema_name>
   AS
BEGIN
    DECLARE LV_ID INTEGER;
    SELECT "ID".NEXTVAL INTO LV_ID FROM DUMMY;

    INSERT INTO "EMPMGMT_EMPLOYEE_DETAILS"
    (
        "ID","NAME","PHONE","EMAIL"
    )
    SELECT :LV_ID,"NAME","PHONE","EMAIL"
    FROM :ST_EMP_DATA;

    COMMIT;
    OUT_SUCCESS := 'SUCCESS'; 
END